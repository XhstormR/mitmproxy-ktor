rootProject.name = "mitmproxy-ktor"

pluginManagement {
    repositories {
        maven("https://mirrors.huaweicloud.com/repository/maven")
        maven("https://maven.aliyun.com/repository/gradle-plugin")
    }
}
